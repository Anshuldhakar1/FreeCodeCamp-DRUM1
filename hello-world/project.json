{
  "name": "hello-world",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "hello-world/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/hello-world"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "hello-world:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "hello-world:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "hello-world:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "hello-world:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "hello-world:build:development"
        },
        "production": {
          "buildTarget": "hello-world:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../coverage/hello-world"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["hello-world/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
